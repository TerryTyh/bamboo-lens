# 个人价值投资APP（MVP版）- 一页纸PRD（开发交付版）

**文档版本**：V1.0 | **撰写人**：产品经理 | **日期**：2026.XX.XX | **适用范围**：MVP版本开发、设计、测试团队 | **接收人**：trae

**核心提醒（开发重点）**：聚焦“选股-收藏-估值-记录”闭环，所有标注【开发重点】的内容需优先落地，确保P0功能无遗漏、无致命bug，重点关注双端适配、数据合规与异常处理。

## 一、产品核心信息（无需开发，仅作认知参考）

- **产品定位**：极简专注的价值投资决策工具，服务长期价值投资者，解决“找好公司、判断贵不贵、记录决策”核心需求，去短线化、去噪音，不引导频繁交易

- **目标用户**：长期价值投资者（持股以年为单位）、关注基本面（ROE/PE/PB等）、有基础财务认知、需要估值判断与决策纪律提醒的个人用户

- **核心目标**：MVP版本实现“选股-收藏-估值-记录”闭环，帮助用户快速发现高价值公司、判断估值合理性

- **差异化亮点**：无短线行情、无噪音信息，以公司价值和估值为核心，极简界面，强化决策纪律与笔记记录

## 二、核心功能需求（P0必做，【开发重点】标注关键项）

### 1. 底部Tab导航（P0，【开发重点】）

- 包含5个核心Tab：首页（估值仪表盘）、价值选股、我的收藏、投资笔记、我的（个人中心）；Tab图标极简线条风格，未选中灰色、选中产品主色（建议深绿色），文字与图标对应，字体一致；Tab高度固定50px，底部无留白，适配不同屏幕（无拉伸、无压缩）。

- 交互要求：切换响应时间≤300ms，仅添加100ms淡入淡出效果；记忆上次打开Tab（首次/清除缓存默认首页）；点击反馈（1.05倍放大后恢复）；卡顿超过500ms显示圆形极简加载动画，加载完成自动切换。

### 2. 首页：估值仪表盘（P0）

- 顶部：APP名称居左、日期居中（YYYY-MM-DD）、全市场估值分位居右（标注低估/合理/高估，搭配浅绿/黄/红色背景），点击分位弹出简要说明（点击空白关闭）；数据加载失败时，3秒后显示“估值数据加载失败，点击重试”。

- 核心模块1（收藏状态概览）：3张卡片横向排列（低估→合理→高估），含状态文字+数量+颜色标识，点击跳转对应收藏分组；数量实时刷新，点击反馈（0.95倍缩小后恢复）；加载失败显示“--”，点击提示重试。

- 核心模块2（重点关注列表）：单条80px，文字左对齐、数值右对齐，显示价值评分（100分制）、PE/PB分位、估值状态圆点、距理想买入价差；支持下拉刷新（极简动画）、上拉加载（每次10条）；加载失败提示重试，无收藏时该模块隐藏。

- 核心模块3（今日重要事件）：按时间倒序，显示公司名称+事件+时间，估值异动事件标对应颜色；无事件显示灰色提示，加载失败提示重试，超过10条可上拉加载。

- 交互：点击公司跳转详情页（150ms过渡动画）；点击事件弹出详情弹窗（含公司入口）；整体可上下滑动，顶部导航固定；网络异常提示对应信息，无更多数据显示“已加载全部”。

### 3. 价值选股（P0）

- 顶部：搜索框（占屏70%，提示“搜索股票名称/代码”，实时联想、支持清空，点击外侧收键盘）；筛选按钮居右；网络异常/加载失败提示对应信息。

- 核心模块1（经典价值策略）：2个卡片横向排列，含策略名称、筛选条件、实时选股数量；点击执行策略（跳转结果页，显示≤1秒“筛选中”提示），点击反馈（0.95倍缩小后恢复）；加载失败显示“--”，筛选失败提示重试。

- 核心模块2（自定义筛选面板）：弹窗占屏70%，底部“重置”“确认”按钮；指标默认无限制，ROE/PE/PB/股息率支持区间输入（非数字/最大值＜最小值提示错误），行业最多勾选5个；点击“重置”恢复默认，“确认”执行筛选，点击外侧不关闭；加载失败提示重试。

- 核心模块3（选股结果列表）：默认按价值评分降序，点击排序可切换（价值评分/ROE/股息率，升序/降序）；单条80px，右侧收藏按钮（空心/实心星，点击提示“已收藏/已取消”）；支持下拉刷新、上拉加载，无结果显示灰色提示；加载失败/收藏操作失败提示对应信息。

- 交互：点击公司跳转详情页；筛选条件自动保存（最多3组常用），可应用/删除；筛选后显示条件摘要，点击可编辑；网络异常提示对应信息。

### 4. 我的收藏（P0）

- 顶部：分组切换（横向滚动）、批量操作按钮居右；分组选中主色+下划线，“全部”分组不可编辑/删除；自定义分组可编辑（最多8字）、删除（提示确认）、拖拽排序；分组加载失败/切换无响应提示重试。

- 核心模块1（收藏列表）：单条90px，左侧公司名称+灰色行业（括号标注），中间价值评分+估值状态+核心财务（ROE/股息率，保留1位小数），右侧“详情/笔记”按钮（触碰显示、离开隐藏）；加载失败提示重试，财务数据失败显示“--”。

- 核心模块2（收藏池统计）：横向3项（总计、行业分布、估值分布），数据实时刷新；加载失败显示“--”，点击可重试。

- 交互：点击“详情”跳转详情页，点击“笔记”跳转对应笔记页（无笔记提示新建）；长按进入批量模式（勾选框、顶部“已选择X家/取消/删除”），删除提示确认；无收藏提示跳转选股页；网络异常提示对应信息。

### 5. 公司详情页（P0，【开发重点】核心页面）

- 顶部：公司名称（加粗）、股票代码+灰色行业（括号标注）；右侧收藏按钮（与收藏池同步，点击提示）、左侧笔记入口（点击跳转笔记页）；导航栏固定，加载失败提示对应信息。

- 核心模块1（价值评分卡片）：圆角矩形（边角8px），显示总分（100分制，保留1位小数）、盈利能力/财务健康分项得分；≥80分绿色、60-79.9分黄色、＜60分红色；加载失败提示“暂无评分数据”。

- 核心模块2（估值温度计，【开发重点】）：宽100%、高200px，横轴近5年（年度标注）、纵轴PE分位（0%-100%）；曲线主色，低估（0%-30%浅绿）、合理（30%-70%浅黄）、高估（70%-100%浅红）背景；当前分位红色圆点标注，点击显示数值；支持左右滑动，加载失败提示重试。

- 核心模块3（估值结论）：卡片式，显示价格状态（加粗+对应颜色）、理想买入价/合理价格（加粗，保留2位小数）、操作建议（1条，匹配PE分位）；加载失败提示“暂无估值结论”。

- 核心模块4（核心财务摘要）：横向4指标（ROE、净利润增速、毛利率、股息率），显示名称+数值（保留1位小数）+趋势箭头（增长绿上、下降红下、持平灰横）；触碰数值显示年度；加载失败对应指标显示“--”。

- 交互：页面可上下滑动，无滑动冲突；网络中断不影响已加载内容，提示对应信息；加载失败提示重试。

### 6. 投资笔记（P0）

- 顶部：分类切换（全部笔记/按公司分类，横向滚动）、“+新建”按钮（主色）；点击“按公司分类”弹出收藏公司列表（无收藏提示无法筛选），筛选后显示筛选信息+清除按钮；加载失败/切换无响应提示重试。

- 核心模块1（笔记列表）：单条自适应（最低80px），显示公司名称（加粗）、时间（YYYY-MM-DD HH:MM）、笔记类型（绿色买入/红色风险/蓝色日常）、摘要（最多2行）；按时间倒序，支持下拉刷新/上拉加载；无笔记显示灰色提示，加载失败提示重试。

- 核心模块2（新建笔记模板）：依次为公司选择（下拉，仅收藏公司）、笔记类型（下拉，默认日常跟踪）、文本编辑框（支持换行/加粗）；底部“保存/取消”，保存需完善信息，取消提示确认；保存失败提示重试，内容不丢失。

- 交互：点击笔记进入详情编辑页（支持修改/删除），删除提示确认；新建保存后跳转列表，新笔记置顶；加载失败提示重试。

### 7. 我的（个人中心，P0）

- 核心功能：手机号验证码登录（6位验证码，60秒重发，错误/手机号不合规提示）；登录状态自动保存（清除缓存需重新登录）；数据云备份（默认凌晨2点自动，支持手动，备份失败提示重试）；3个消息提醒开关（估值/财报/分红，默认开启，状态实时保存）。

- 基础功能：首次登录弹出风险声明（必须同意才可进入，后续可在个人中心查看）；意见反馈（提交失败提示，内容不丢失）；关于APP（版本号、更新日志、联系方式）；设置（深色/浅色模式跟随系统，支持版本检测、手动清除缓存，与数据缓存机制联动）；iOS端推送（授权后接收提醒），Android端适配系统推送；加载失败/操作无响应提示重试。

## 三、非功能需求（P0，【开发重点】标注关键项）

- **性能（【开发重点】）**：冷启动≤3秒、热启动≤1.5秒；页面加载≤2秒，筛选/切换Tab响应≤300ms；数据更新延迟≤12小时；后台驻留不重启、无数据丢失；连续操作30分钟无闪退/内存泄漏；估值温度计加载≤1秒，无卡顿。

- **合规**：全APP显著位置展示免责声明（不构成投资建议、不荐股），无实盘交易入口。

- **兼容性（【开发重点】）**：支持iOS13.0+/Android8.0+，覆盖主流机型（4.7-6.8英寸竖屏）；适配双端深色模式、系统字体缩放（100%-150%），无拉伸/遮挡/文字溢出。

- **隐私**：用户数据AES加密存储，本地缓存仅存设备，不收集无关数据；仅申请网络/存储权限（存储可关闭，不影响核心功能）；退出登录清除当前设备缓存，与缓存机制、登录功能联动。

## 四、数据需求（P0，【开发重点】）

- 基础数据：股票名称、代码、行业、近5年PE分位、近3年核心财务（ROE、净利润、毛利率、股息率）

- 动态数据：财报时间、分红公告、股票最新价格（日间更新）

- 计算数据：价值评分（ROE+财务健康度）、理想买入价/合理价格（PE分位计算）

- 数据来源（【开发重点】）：优先对接持牌财经接口（东方财富/同花顺等），仅调取核心数据；iOS缓存存沙盒、Android存应用目录（单用户≤100MB），支持手动清除；数据日间更新（延迟≤12小时），缓存与接口实时同步，用户数据云备份；接口加载失败优先显示缓存，无缓存显示“暂无数据”；MVP用单一主接口，后续迭代预留备用接口。

## 五、MVP版本范围（明确不含，无需开发）

- 复杂估值模型、行业对比；详细研报、股吧、行情K线等短线功能；批量笔记编辑、复杂图表、多设备同步（除基础云备份）；自定义估值区间、高级筛选条件。

## 六、上线标准（P0，【开发重点】，验收依据）

- 所有P0功能正常，无致命bug、无卡顿；数据更新/估值计算准确，财务数据无错误。

- 合规声明完整，隐私保护达标，适配主流机型；核心路径（选股-收藏-看估值-写笔记）无断点。

- 双端交互规范（iOS手势返回、Android按键返回）；无交互异常/文字溢出/布局错乱；推送、缓存正常，权限申请合理。
> （注：文档部分内容可能由 AI 生成）