# 竹鉴 Codex 协作规则

## 项目定位

竹鉴不是新闻聚合器，而是长期投资研究系统。目标是把公司原文、财务证据、业务判断、估值框架和后续验证沉淀为可复用的正式研究资产。

## 开始工作前

1. 阅读根目录 `README.md` 和 `.codex/PROJECT.md`。
2. 查看 `git status --short`，不要覆盖用户或其他自动化产生的修改。
3. 运行 `git pull --rebase` 同步 GitHub Actions 的最新产物。
4. 查看最新日报、自动化健康报告和候选池，再选择任务。

## 关键边界

- `研究门户/`：用户可见界面和门户导出数据。
- `云端研究简报系统/`：自动化脚本、配置和云端输出。
- `长期高潜力公司跟踪系统/`：稳定规则与历史知识，不作为每日操作日志。
- `资料/`：原始输入，不直接等同于研究结论。
- `docs/`：产品和工程说明，不放公司正式事件。

## 研究规则

- 标题型候选不得进入正式事件、首页最近更新或日报关键变化。
- 正式事件必须读原文，并具备具体证据、业务影响、估值或动作影响和验证清单。
- A 股复杂制造优先看客户集中度、毛利率、经营现金流、存货、应收和资本开支。
- 公司研究动作与个人组合仓位分开；没有用户最新资产数据时，不给具体买入金额。
- 不因主题热度或单日涨跌升层，不虚构估值区间。

## 常用验证

- JavaScript：使用可用 Node 运行 `node --check`。
- Python：运行相关脚本的语法或最小数据验证。
- 门户：确认正式事件进入 `event-store-data.js`，候选状态变为 `promoted`，首页选择逻辑能展示。
- Git：提交前运行 `git diff --check`，推送前处理远端自动化提交。

## 提交约定

- 自动化产物与手工研究尽量分开提交。
- 不提交只有标题、空摘要或占位词的正式草稿。
- 推送后如需触发工作流，等待完成并拉取机器人提交，再核验最终状态。
